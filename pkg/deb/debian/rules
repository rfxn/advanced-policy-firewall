#!/usr/bin/make -f
# apf Debian rules — generated by pkg_gen.sh from pkg_lib templates
#
# Placeholders:
#   apf                    — package name
#   @@PKG_DEB_BUILD_COMMANDS@@      — FHS path transform commands for override_dh_auto_build
#   @@PKG_DEB_INSTALL_COMMANDS@@    — install commands for override_dh_auto_install
#   @@PKG_DEB_CLEAN_COMMANDS@@      — cleanup commands for override_dh_auto_clean
#
%:
	dh $@

override_dh_auto_build:
@@PKG_DEB_BUILD_COMMANDS@@

override_dh_auto_install:
@@PKG_DEB_INSTALL_COMMANDS@@

override_dh_auto_test:
	# Skip — tests require privileged Docker environment

override_dh_auto_clean:
@@PKG_DEB_CLEAN_COMMANDS@@
